# Story 2.3: Panel Template and Component Refresh

## Status
Ready for Review

## Story
**As a** user,
**I want** panels, buttons, modals, sliders, and inputs to follow the glassmorphism patterns with translucent surfaces, white-at-opacity borders, and tight spacing,
**so that** all interactive UI elements are visually cohesive with the new design language.

## Acceptance Criteria
1. **Buttons -- primary:** `var(--color-primary)` background, `var(--text-primary)` text, `border-radius: var(--radius-lg)`, `hover: opacity 0.9`, `disabled: opacity 0.5`
2. **Buttons -- ghost:** `border: 1px solid var(--border-default)`, transparent background, `var(--text-secondary)` text, `hover: var(--surface-accent)` background
3. **Modals:** overlay uses `oklch(0.04 0 0 / 0.8)` with `backdrop-filter: blur(4px)`; dialog panel uses `var(--surface-card-80)`, `var(--border-default)`, `border-radius: var(--radius-xl)`, padding 24px, `backdrop-filter: blur(12px)`
4. **Text inputs:** `var(--color-background)` background, `var(--border-default)` border, `border-radius: var(--radius-lg)`, `focus: ring 2px var(--color-primary)`, `placeholder: var(--text-muted)`
5. **Sliders:** track styled with `var(--surface-card-80)`, thumb uses `var(--color-primary)`, value display uses `var(--text-secondary)` at `--font-size-xs`
6. **Status badges:** `font-size: 11px`, `background: oklch(1 0 0 / 0.06)`, `padding: 2px 8px`, `border-radius: 9999px`, `color: var(--text-muted)`
7. **Section headers (sticky group dividers):** `font-size: 10px`, `text-transform: uppercase`, `letter-spacing: 0.05em`, `color: var(--text-muted)`, `background: oklch(1 0 0 / 0.05)`
8. **Selection lists:** `max-height` with `overflow-y: auto`, `border: 1px solid var(--border-default)`, rows with `padding: 10px 16px`, `hover: var(--surface-accent)`, `border-bottom: 1px solid var(--border-subtle)`, last row no border; selected state: `oklch(0.55 0.15 150 / 0.2)` background
9. **Panel number badge:** 24px circle, `var(--surface-card-80)` background, `var(--color-primary)` text, subtle green glow on panel hover
10. **Panel summary row:** tight padding (`var(--gap-sm)` vertical, `var(--gap-md)` horizontal), flex layout with `gap: var(--gap-sm)`
11. **Error messages:** `var(--color-error)` left border (4px), `oklch(0.6 0.2 25 / 0.1)` background, `var(--text-primary)` text
12. **Button-in-hole pattern:** replaced with flat glassmorphism buttons (the skeuomorphic bevel/hole pattern is removed entirely)
13. All component colors use oklch() or reference oklch-based CSS variables -- no hex/hsl

## Tasks / Subtasks
- [x] **Task 1: Rewrite button styles in components.css** (AC: 1, 2, 12)
  - [x] Remove `.btn-hole` and `.btn-surface` skeuomorphic pattern
  - [x] Create `.btn-primary` flat green style
  - [x] Create `.btn-ghost` bordered transparent style
  - [x] Update all button hover/active/disabled states
  - [x] Transitions: color/opacity/background only, 150ms
- [x] **Task 2: Rewrite modal styles in components.css** (AC: 3)
  - [x] Modal overlay: dark translucent with subtle blur
  - [x] Modal dialog: glass panel with blur, border, rounded corners
  - [x] Modal entrance: scale(0.95) -> scale(1), 200ms
  - [x] Close button: ghost style, 18-20px icon
- [x] **Task 3: Restyle form inputs** (AC: 4)
  - [x] Text inputs, number inputs, selects: dark background, white-at-opacity border
  - [x] Focus state: 2px ring in primary color
  - [x] Placeholder text in muted color
- [x] **Task 4: Restyle sliders** (AC: 5)
  - [x] Range input track: surface-card-80
  - [x] Range input thumb: primary green
  - [x] Value display: xs font, secondary text color
  - [x] Update both `slider.html` and `slider-collapsed.html` template styles
- [x] **Task 5: Create utility component styles** (AC: 6, 7, 8)
  - [x] Status badge pattern
  - [x] Section header pattern
  - [x] Selection list pattern with hover and selected states
- [x] **Task 6: Update panel chrome** (AC: 9, 10, 11)
  - [x] Panel number badge: green-tinted circle
  - [x] Summary row: tightened padding and gaps
  - [x] Error message: red accent left border with translucent background
  - [x] Play/pause, delete, duplicate button styling
- [x] **Task 7: Final audit** (AC: 13)
  - [x] Grep components.css and panels.css for hex/hsl
  - [x] Verify all colors are oklch or variable references

## Dev Notes

### Current Button Pattern (to be removed)
The existing UI uses a skeuomorphic "button-in-hole" pattern:
```css
.btn-hole { /* outer recessed container */ }
.btn-surface { /* raised inner button with bevel lighting */ }
```
This is being replaced entirely with flat glassmorphism buttons. The `.btn-hole` and `.btn-surface` classes and their CSS can be deleted. Any HTML using these classes will need the class names updated to `.btn-primary` or `.btn-ghost`.

### Key Files
- `static/css/components.css` (~400 lines -- major rewrite)
- `static/css/panels.css` (~250 lines -- moderate updates)
- `public/skins/default/templates/panel.html` (may need class name updates if button classes change)
- `public/skins/default/templates/slider.html`
- `public/skins/default/templates/slider-collapsed.html`

### Template Class Dependencies
The JavaScript expects these classes to exist (from skinManager exploration):
- `.level-panel`, `.panel-number-badge`, `.panel-title`
- `.panel-actions-left`, `.panel-actions`
- `.btn-playback`, `.btn-delete`, `.btn-duplicate`
- `.code-editor`, `.code-editor-wrapper`, `.error-message`
- `.panel-editor-container`, `.panel-controls-container`
- `.leaf-slider`, `.slider-value`

These class names MUST be preserved. New styling classes can be added, but functional classes cannot be renamed without updating JS.

### Technical Constraints
- Range input styling requires vendor-prefixed pseudo-elements: `::-webkit-slider-thumb`, `::-webkit-slider-runnable-track`, `::-moz-range-thumb`, `::-moz-range-track`
- Modal scale animation requires `transform` transition -- this is the ONE allowed exception to the "no layout animations" rule
- Button class changes in CSS must match any class references in panel templates and `index.html`

## Testing
- Verify all buttons render correctly: primary (green fill), ghost (bordered)
- Open settings modal: verify blur overlay, glass panel, scale-in animation
- Interact with sliders: verify track/thumb colors, value display
- Check panel summary row spacing is tight and consistent
- Trigger an error: verify red-accented error message display
- Verify no visual regression on panel play/pause/delete functionality

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Completion Notes
- **components.css**: Full rewrite from 1552 lines to ~850 lines. Removed entire skeuomorphic button-in-hole system (`.btn-hole`, `.btn-surface`, `.btn-raised`, all bevel/shadow variants). Replaced with flat glassmorphism: `.btn-primary` (green fill, opacity hover), `.btn-ghost`/`.btn-secondary` (bordered transparent, surface-accent hover), `.btn-icon` (minimal toolbar button).
- **panels.css**: Full rewrite from 552 lines to ~420 lines. All hex colors replaced with oklch tokens.
- Hero buttons: converted from bevel+shadow to flat translucent surfaces with border-default
- All buttons (stop-all, update-all, add-panel, pause, activate, delete, control): converted to oklch semantic colors
- Modal overlay: `oklch(0.04 0 0 / 0.8)` with `blur(4px)`, dialog: `surface-card-80` with `blur(12px)`, `@keyframes modal-enter` scale(0.95→1) 200ms
- Inputs (text, number, select): `--color-background` bg, `--border-default` border, focus ring `0 0 0 2px var(--color-primary)`, placeholder `--text-muted`
- Sliders: track `--surface-card-80`, thumb `--color-primary`, value display `--font-size-xs` + `--text-secondary`
- New utility classes: `.status-badge` (pill, 11px, oklch bg), `.section-header` (10px uppercase), `.selection-list` (max-height, border, hover/selected states)
- Panel badge: 24px circle, `--surface-card-80` bg, `--color-primary` text, green glow on hover
- Summary row: tightened to `gap-sm` vertical / `gap-md` horizontal
- Error messages: 4px `--color-error` left border, `oklch(0.6 0.2 25 / 0.1)` background, `--text-primary` text
- Toggles: converted from burnt-red accent to `--color-primary`
- All transitions: 150ms on color/opacity/background-color only
- Confirmation modal: same glass treatment with blur + scale animation
- No template changes needed — functional class names preserved
- Zero hex/rgba/hsl in either file (verified by grep)
- Build succeeds cleanly

### File List
- `static/css/components.css` — full rewrite
- `static/css/panels.css` — full rewrite
- `index.html` — replaced btn-hole/btn-surface on skin import button with btn-ghost
- `static/css/variables.css` — removed dead button-in-hole CSS variables
- `static/css/accessibility.css` — removed .btn-hole selector, converted all hex to oklch

## QA Results

### Review Date: 2026-02-23

### Reviewed By: Quinn (Test Architect)

**AC Traceability:** 11/13 ACs fully verified against components.css and panels.css. AC-12 and AC-13 have medium-severity remnants in adjacent files.

**Findings:**
- REQ-001 (medium): `index.html:282-283` still uses `btn-hole`/`btn-surface` classes — skin import button renders unstyled
- MNT-001 (medium): `variables.css:175-183,216` defines dead `--btn-hole-*`/`--btn-surface-*` CSS variables
- MNT-002 (medium): `accessibility.css:100,159-161` references orphaned `.btn-hole` selector and overrides
- MNT-003 (low): `accessibility.css` contains hex color values not yet converted to oklch

**NFR:** Security N/A, Performance PASS, Reliability PASS, Maintainability CONCERNS

### Gate Status

Gate: CONCERNS → specs/qa/gates/2.3-panel-and-component-refresh.yml

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-23 | 1.0 | Full rewrite of components.css and panels.css to flat glassmorphism; removed skeuomorphic button-in-hole pattern; all oklch colors; added status-badge, section-header, selection-list utilities | James (Claude Opus 4.6) |
| 2026-02-23 | 1.1 | QA fix: REQ-001 index.html btn-hole→btn-ghost, MNT-001 removed dead vars from variables.css, MNT-002/003 rewrote accessibility.css removing btn-hole refs and converting all hex to oklch | James (Claude Opus 4.6) |
