# Story 1.4: GitHub Pages Deployment (MkDocs + Lite App)

## Status
Done

## Story
**As a** maintainer,
**I want** the documentation site and lite web app to rebuild and deploy to GitHub Pages on every push to `main`,
**so that** the public site at r0astr.org stays current with the latest docs and app version.

## Acceptance Criteria
1. Reusable workflow at `.github/workflows/deploy-pages.yml` accepting `workflow_call` (also triggerable standalone on push to `main`)
2. Builds MkDocs Material site: Python 3.x setup, `pip install mkdocs-material`, `mkdocs build --site-dir _site`
3. Builds lite app: Node.js 20 with npm cache, `npm ci`, `npm run build:lite`, copies `dist-lite/` contents into `_site/app/`
4. Deploys combined `_site/` to GitHub Pages via `actions/upload-pages-artifact` + `actions/deploy-pages@v4`
5. Uses current stable Python 3.x -- no version pinning to 3.11; installs `setuptools` via pip if needed for node-gyp compatibility
6. Caches pip dependencies (`~/.cache/pip`) and npm dependencies (`~/.npm`) for faster builds
7. Docs source is `docs-source/` directory as configured in `mkdocs.yml`
8. CNAME file for `r0astr.org` is preserved in the deployed `_site/` root

## Tasks / Subtasks
- [x] **Task 1: Create deploy-pages.yml reusable workflow** (AC: 1)
  - [x] Define `workflow_call` trigger (for orchestrator)
  - [x] Define `push` trigger on `main` branch as standalone fallback
  - [x] Define `workflow_dispatch` for manual trigger
  - [x] Set `permissions: pages: write, id-token: write` for Pages deployment
- [x] **Task 2: Implement MkDocs build step** (AC: 2, 5, 7)
  - [x] Setup Python 3.x (no pinned minor version)
  - [x] Cache pip dependencies
  - [x] `pip install mkdocs-material`
  - [x] `pip install setuptools` (for node-gyp if needed)
  - [x] `mkdocs build --site-dir _site`
- [x] **Task 3: Implement lite app build step** (AC: 3, 6)
  - [x] Setup Node.js 20 with npm cache
  - [x] `npm ci`
  - [x] `npm run build:lite`
  - [x] Copy `dist-lite/*` into `_site/app/`
- [x] **Task 4: Deploy to GitHub Pages** (AC: 4, 8)
  - [x] Ensure CNAME file exists in `_site/` (from `docs-source/` or explicit copy)
  - [x] Use `actions/upload-pages-artifact@v3` to upload `_site/`
  - [x] Use `actions/deploy-pages@v4` to deploy
  - [x] Configure environment: `github-pages` with URL output

## Dev Notes

### Current State
- Existing `deploy-pages.yml` works but has fragility issues:
  - Pins Python to 3.11 for distutils compatibility
  - Runs `rm -rf node_modules package-lock.json && npm install` instead of `npm ci`
  - No dependency caching
- MkDocs config: `mkdocs.yml` at repo root, docs in `docs-source/`, output to `_site`
- Lite build config: `vite.config.lite.mjs` with base path `/app/`
- Site URL: `https://r0astr.org` (custom domain via CNAME)

### Key Files
- `.github/workflows/deploy-pages.yml` (rewrite)
- `mkdocs.yml` (existing, unchanged)
- `vite.config.lite.mjs` (existing, unchanged)
- `docs-source/` (MkDocs source files)
- `docs-source/CNAME` (if exists, or needs to be created)

### Technical Constraints
- GitHub Pages deployment requires the `github-pages` environment and OIDC token
- `actions/deploy-pages` requires `actions/configure-pages` to be called first
- The lite build aliases `websocketManager` to a lite stub (`websocketManager.lite.js`) -- this is handled by `vite.config.lite.mjs`
- `npm ci` requires committed `package-lock.json` -- verify it is tracked and up to date
- MkDocs Material may need additional plugins installed depending on `mkdocs.yml` config (check `plugins:` section)

### Site Structure After Deploy
```
r0astr.org/
  index.html          # MkDocs home
  getting-started/    # MkDocs pages
  learn/              # MkDocs pages
  app/                # Lite web app
    index.html        # Vite SPA
    assets/           # Bundled JS/CSS
  CNAME               # Custom domain
```

## Testing
- Push a docs-only change to `main` and verify site rebuilds
- Verify `r0astr.org` loads the MkDocs site
- Verify `r0astr.org/app/` loads the lite web app
- Verify pip and npm caches are populated on second run (check cache hit in logs)

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Completion Notes
- Rewrote existing `deploy-pages.yml` — kept same filename so no downstream references break
- Python `3.x` replaces pinned `3.11`; `setuptools` installed alongside `mkdocs-material` in single pip command
- `npm ci` replaces destructive `rm -rf node_modules && npm install` hack
- pip caching via `setup-python` `cache: "pip"`, npm caching via `setup-node` `cache: "npm"` (AC6)
- `docs-source/CNAME` exists in repo — MkDocs copies non-markdown files from `docs_dir` to output, so CNAME lands in `_site/` automatically
- Added CNAME verification step that warns if file missing after MkDocs build
- `configure-pages@v5` called before `upload-pages-artifact@v3` per GitHub Pages requirement
- Concurrency group `"pages"` with `cancel-in-progress: false` preserved from original workflow
- `fetch-depth: 0` removed from checkout — not needed for pages build (no tag/history operations)
- MkDocs plugins only `search` — no additional pip packages required
- **QA fix: PERF-001** — Created `requirements.txt` at repo root with `mkdocs-material` and `setuptools`; workflow now uses `pip install -r requirements.txt` so `setup-python cache: pip` has a file to hash for the cache key

### File List
| File | Action |
|------|--------|
| `.github/workflows/deploy-pages.yml` | Rewritten |
| `requirements.txt` | Created |
| `specs/stories/epic-cicd/1.4.github-pages-deployment.md` | Modified |

## QA Results

### Review Date: 2026-02-22

### Reviewed By: Quinn (Test Architect)

**All 8 acceptance criteria addressed. AC6 (pip caching) partially met.**

| Finding | Severity | Summary |
|---------|----------|---------|
| PERF-001 | medium | Pip cache inactive — `setup-python cache: pip` needs a `requirements.txt` (none exists) |

### Re-Review Date: 2026-02-22

Finding resolved in v1.1:
- **PERF-001**: `requirements.txt` created, `pip install -r requirements.txt` used — cache active ✅

### Gate Status

Gate: PASS → specs/qa/gates/1.4-github-pages-deployment.yml

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-22 | 1.0 | Rewrite deploy-pages.yml: workflow_call trigger, Python 3.x, npm ci, pip/npm caching, CNAME verification | James (Claude Opus 4.6) |
| 2026-02-22 | 1.1 | QA fix: PERF-001 — added requirements.txt for pip cache activation | James (Claude Opus 4.6) |
