# Story 1.2: Multi-Arch Electron Build Matrix

## Status
Done

## Story
**As a** maintainer,
**I want** a reusable build workflow that produces signed Electron artifacts for macOS (Intel + ARM), Windows x64, and Linux x64 in parallel,
**so that** releases include all platform binaries built consistently from the same source.

## Acceptance Criteria
1. Reusable workflow at `.github/workflows/build-electron.yml` accepting version string as input
2. Build matrix: `macos-latest` (dmg + zip, universal or x64 + arm64), `windows-latest` (exe, x64), `ubuntu-latest` (AppImage + deb, x64)
3. Each job: checkout, Node.js 20 setup with npm cache, `npm ci`, version injection into `package.json`, platform-specific `electron-builder` invocation
4. macOS builds use existing `build-resources/entitlements.mac.plist` and codesigning secrets from environment
5. All artifacts uploaded via `actions/upload-artifact@v4` with naming convention `electron-{platform}-{arch}`
6. Builds use `npm ci` with lockfile -- no destructive `rm -rf node_modules` hack
7. Build failures on one platform do not cancel other platform builds (`fail-fast: false`)

## Tasks / Subtasks
- [x] **Task 1: Create build-electron.yml reusable workflow** (AC: 1, 2, 7)
  - [x] Define `workflow_call` with `version` input
  - [x] Define build matrix with OS, platform flags, and artifact names
  - [x] Set `fail-fast: false` on matrix strategy
- [x] **Task 2: Implement per-platform build steps** (AC: 3, 6)
  - [x] Checkout with full history
  - [x] Setup Node.js 20 with `npm` cache
  - [x] Run `npm ci` (no clean hacks)
  - [x] Inject version into `package.json` via `npm version --no-git-tag-version`
  - [x] Run `electron-builder` with platform-specific flags
- [x] **Task 3: macOS code signing** (AC: 4)
  - [x] Import codesigning certificate from secrets
  - [x] Set `CSC_LINK` and `CSC_KEY_PASSWORD` environment variables
  - [x] Apply entitlements from `build-resources/entitlements.mac.plist`
  - [x] Build for both x64 and arm64 (or universal)
- [x] **Task 4: Artifact upload** (AC: 5)
  - [x] Upload platform artifacts with consistent naming
  - [x] Include only distributable files (dmg, zip, exe, AppImage, deb)
  - [x] Exclude build intermediates

## Dev Notes

### Current State
- Existing `release.yml` has three separate jobs: `build-mac`, `build-windows`, `build-linux`
- macOS build uses `macos-13` (Intel) -- consider `macos-latest` (ARM) or `macos-14` for Apple Silicon native builds
- Current workflow deletes `node_modules` and `package-lock.json` before install -- this was a workaround for rollup optional dependency issues that `npm ci` should handle correctly
- Windows and Linux builds are straightforward

### Key Files
- `.github/workflows/build-electron.yml` (new)
- `electron-builder.json` (existing build config)
- `build-resources/entitlements.mac.plist` (existing)
- `build-resources/icons/` (existing platform icons)
- `package.json` (version field to inject)

### Technical Constraints
- `electron-builder` reads `electron-builder.json` for platform config
- macOS notarization requires Apple ID credentials if enabled (check current setup)
- `npm ci` requires `package-lock.json` to be committed and in sync with `package.json`
- GitHub-hosted macOS runners: `macos-latest` is now ARM (M1) -- for x64+arm64 cross-compilation, use `--mac --x64 --arm64` flags on electron-builder
- Artifact retention: default 90 days is fine for release assets

### electron-builder.json Reference
```json
{
  "appId": "com.piatra.r0astr",
  "productName": "r0astr",
  "directories": { "output": "release" },
  "mac": { "target": ["dmg", "zip"], "category": "public.app-category.music" },
  "win": { "target": ["nsis", "portable"] },
  "linux": { "target": ["AppImage", "deb"] }
}
```

## Testing
- Trigger workflow manually with a test version string
- Verify all three platform jobs complete independently
- Download artifacts and verify file names and sizes are reasonable
- Verify macOS artifacts are signed (if certs available in test environment)

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Completion Notes
- Single-job matrix strategy with `fail-fast: false` — 3 platform builds run independently
- macOS builds on `macos-latest` (ARM runner) with electron-builder cross-compiling x64+arm64 per existing `electron-builder.json` config
- Entitlements applied automatically via `electron-builder.json` mac.entitlements reference — no separate import step needed
- Code signing via `CSC_LINK`/`CSC_KEY_PASSWORD` secrets passed as env vars; warning emitted if certs not configured (unsigned build still succeeds)
- Apple notarization secrets included (`APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `APPLE_TEAM_ID`) for when notarization is enabled
- Python 3.12 + setuptools included for node-gyp native module compilation (carried forward from existing release.yml)
- Version injection uses `npm version --no-git-tag-version --allow-same-version` to handle re-runs cleanly
- Per-platform artifact upload steps with explicit glob patterns to exclude build intermediates (no `release/**/*` catch-all)
- Artifact naming: `electron-macos-universal`, `electron-windows-x64`, `electron-linux-x64`
- **QA fix: SEC-002** — Replaced `${{ inputs.version }}` direct interpolation with `$VERSION` env var in npm version command
- **QA fix: ARCH-002** — Renamed macOS artifact from `electron-macos` to `electron-macos-universal` to match AC5 `electron-{platform}-{arch}` convention
- **QA fix: MNT-002** — Renamed step from "Import macOS signing certificate" to "Verify macOS signing certificate"

### File List
| File | Action |
|------|--------|
| `.github/workflows/build-electron.yml` | Created |
| `specs/stories/epic-cicd/1.2.multi-arch-electron-build.md` | Modified |

## QA Results

### Review Date: 2026-02-22

### Reviewed By: Quinn (Test Architect)

**All 7 acceptance criteria functionally met.**

| Finding | Severity | Summary |
|---------|----------|---------|
| SEC-002 | medium | `${{ inputs.version }}` direct interpolation on line 54 — use `$VERSION` env var instead |
| ARCH-002 | low | macOS artifact named `electron-macos` instead of `electron-macos-{arch}` per AC5 |
| MNT-002 | low | "Import macOS signing certificate" step name misleading — rename to "Verify" |

### Re-Review Date: 2026-02-22

All three findings resolved in v1.1:
- **SEC-002**: `$VERSION` env var used instead of direct interpolation ✅
- **ARCH-002**: macOS artifact renamed to `electron-macos-universal` ✅
- **MNT-002**: Step renamed to "Verify macOS signing certificate" ✅

### Gate Status

Gate: PASS → specs/qa/gates/1.2-multi-arch-electron-build.yml

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-22 | 1.0 | Initial implementation of build-electron reusable workflow with matrix strategy, code signing, and artifact upload | James (Claude Opus 4.6) |
| 2026-02-22 | 1.1 | QA fix: SEC-002 (env var for version input), ARCH-002 (electron-macos-universal naming), MNT-002 (step rename) | James (Claude Opus 4.6) |
